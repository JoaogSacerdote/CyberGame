# Nível mínimo exigido do CMake
cmake_minimum_required(VERSION 3.16)

# Inclui as ferramentas padrão do ESP-IDF
include($ENV{IDF_PATH}/tools/cmake/project.cmake)

# Nome do seu projeto
project(CyberGame)